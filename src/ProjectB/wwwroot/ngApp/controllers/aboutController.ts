namespace ProjectB.Controllers {
    export class AboutController {
        public quote

        constructor(private aboutService: ProjectB.Services.AboutService)
        {
            //this.quote = aboutService.getRandomQuote();
        }
    }
}
